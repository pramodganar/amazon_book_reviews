# fitted objects are reproducible from train.py; the hosted Streamlit demo needs a
# few at runtime, so those are force-tracked and everything else here is ignored.
artifacts/*
!artifacts/feature_pipeline.joblib
!artifacts/kmeans.joblib
!artifacts/config.json
!artifacts/cluster_meta.json
!artifacts/scatter.parquet

# raw data is large (~260MB) — not committed
data/raw/*.csv
data/raw/*.db

# python
__pycache__/
*.pyc
.venv/
.ipynb_checkpoints/
.pytest_cache/
